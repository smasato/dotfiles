git:
  paging:
    colorArg: always
    # side-by-side is enabled globally in ~/.gitconfig but is too cramped in
    # lazygit's half-width pane, so disable it here.
    pager: delta --dark --paging=never --side-by-side=false
